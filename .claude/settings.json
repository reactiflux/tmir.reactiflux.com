{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "timeout": 30,
            "statusMessage": "prettier",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); [ -n \"$f\" ] && npx prettier --write --ignore-unknown --log-level warn \"$f\""
          },
          {
            "type": "command",
            "timeout": 30,
            "statusMessage": "react-doctor",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.ts|*.tsx|*.js|*.jsx|*.mjs) out=$(npx react-doctor --no-telemetry --no-supply-chain --blocking warning \"$f\" 2>&1) || { printf '%s\\n' \"$out\" | sed -n '/^[0-9][0-9]* issues*$/,/────/p' >&2; exit 2; };; esac"
          }
        ]
      }
    ]
  }
}
